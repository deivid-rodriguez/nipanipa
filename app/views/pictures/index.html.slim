- provide :title, t(".title")

= render layout: "shared/profile", locals: { user: @user } do
  - if @user == current_user
    p.new-picture
      = link_to t("pictures.index.new_picture"),
                new_user_picture_path(current_user)

  = render "pictures", pictures: @pictures, user: @user

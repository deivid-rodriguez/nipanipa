---

checkout:
  post:
    - git config --local user.email 'circle@circle.ci'
    - git config --local user.name 'Circle CI'

test:
  pre:
    - bin/overcommit --sign

  post:
    - bin/overcommit --run

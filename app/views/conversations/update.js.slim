| $('div.alert').remove();
| $('body main').prepend("#{j render 'layouts/messages'}");
| $('.message-action').find('input').show();
| $('.message-action').find('img').hide();

- if @message.errors.empty?
  | $("#{j render('sent', message: @message)}").insertBefore('.reply-box');
  | $('textarea.message-body').val('');

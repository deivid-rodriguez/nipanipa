<% if pictures.empty? %>
  <div class="no-elements"  >
    <em><%= t('pictures.nopictures') %></em>
  </div>
<% end %>
<% pictures.each do |p| %>
  <%= link_to user_pictures_path(p.user, anchor: "image-#{p.id}") do %>
    <%- image_tag p.image.url(:small) %>
  <% end %>
<% end %>

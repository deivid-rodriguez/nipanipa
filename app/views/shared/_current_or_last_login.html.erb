<!-- This doesn't work because trackable doesn't work as I expected
     It could be changed to work (i think) by adding a Warden's before_logout
     hook -->
<% if false %>
  <% if user.current_sign_in_at %>
    <%= t '.currently_logged_in' %>
  <% end %>
<% else %>
    <%= t '.last_login' %>:
    <%= @last_location %>,
    <%= t 'datetime.timestamp_ago',
          time: time_ago_in_words(user.last_sign_in_at) %><br>
<% end %>

| $("#conversation-preview-#{params[:id]}").remove();

- no_messages_div = j render("shared/empty", message: "nomessages")

| if ($("#conversation-list").is(":empty"))
|   { $("#conversation-list").replaceWith("#{no_messages_div}") }

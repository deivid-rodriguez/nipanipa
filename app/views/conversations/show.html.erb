<% provide :title, @conversation.subject %>

<div class="col-md-12">
  <div class="row main-titled">

    <div class="offset2 col-md-7 title">
      <h3><%= @conversation.subject %></h3>
    </div>

    <div class="col-md-1 back">
      <%= link_to t('.back_html'), conversations_path %>
    </div>

    <div class="offset2 col-md-8 chat-list">
      <ul id="chat-list">
        <% for message in @conversation.messages %>
        <li>
          <%= render message %>
        </li>
        <% end %>
      </ul>
    </div>

    <div class="offset2 col-md-8 message-reply">
      <%= form_tag reply_conversation_path(@conversation),
                   method: :put, remote: true do %>
        <%= text_area_tag :body %>
        <%= submit_tag t('.reply'), class: "btn btn-small pull-right" %>
      <% end %>
    </div>

  </div>

</div>
